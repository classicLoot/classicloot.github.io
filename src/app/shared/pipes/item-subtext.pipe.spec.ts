import { ItemSubtextPipe } from './item-subtext.pipe';

describe('ItemSubtextPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemSubtextPipe();
    expect(pipe).toBeTruthy();
  });
});
