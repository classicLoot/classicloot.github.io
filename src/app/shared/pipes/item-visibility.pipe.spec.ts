import { ItemVisibilityPipe } from './item-visibility.pipe';

describe('ItemVisibilityPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemVisibilityPipe();
    expect(pipe).toBeTruthy();
  });
});
